name: test
on:
  - workflow_call
jobs:
  test-e2e:
    strategy:
      matrix:
        os: ['ubuntu-latest']
        node-version: [16.x, 18.x]
    runs-on: ${{ matrix.os }}
    env:
      PLANET_SCALE_TEST_DB_URI: ${{ secrets.PLANET_SCALE_TEST_DB_URI }}
      TEST_ENV: test_value_2
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: yarn
      - run: yarn -s test:db:up
      - run: echo "Planet Scale test  "
      - run: echo ${{ secrets.PLANET_SCALE_TEST_DB_URI }}
      - uses: prisma/action-node-cache-dependencies@main
      - uses: ./.github/actions/generateTemplates
      - run: echo "uri:$PLANET_SCALE_TEST_DB_URI" & yarn -s test:e2e ${{ secrets.PLANET_SCALE_TEST_DB_URI }}
