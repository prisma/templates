export const SaasWithSqlserverWithReferentialIntegrityPrisma = [
  "\n\n-- CreateTable\nCREATE TABLE [dbo].[User] (\n    [id] NVARCHAR(1000) NOT NULL,\n    [createdAt] DATETIME2 NOT NULL CONSTRAINT [User_createdAt_df] DEFAULT CURRENT_TIMESTAMP,\n    [updatedAt] DATETIME2 NOT NULL,\n    [name] NVARCHAR(1000),\n    [email] NVARCHAR(1000) NOT NULL,\n    [accountId] NVARCHAR(1000),\n    CONSTRAINT [User_pkey] PRIMARY KEY ([id])\n)",
  "\n\n-- CreateTable\nCREATE TABLE [dbo].[Account] (\n    [id] NVARCHAR(1000) NOT NULL,\n    [createdAt] DATETIME2 NOT NULL CONSTRAINT [Account_createdAt_df] DEFAULT CURRENT_TIMESTAMP,\n    [updatedAt] DATETIME2 NOT NULL,\n    [stripeCustomerId] NVARCHAR(1000) NOT NULL,\n    [stripeSubscriptionId] NVARCHAR(1000) NOT NULL,\n    [referrer] NVARCHAR(1000),\n    [isActive] BIT NOT NULL,\n    CONSTRAINT [Account_pkey] PRIMARY KEY ([id])\n)",
  "\n\n-- CreateTable\nCREATE TABLE [dbo].[Invite] (\n    [id] NVARCHAR(1000) NOT NULL,\n    [dateSent] DATETIME2 NOT NULL CONSTRAINT [Invite_dateSent_df] DEFAULT CURRENT_TIMESTAMP,\n    [email] NVARCHAR(1000) NOT NULL,\n    [accountId] NVARCHAR(1000),\n    [isValid] BIT NOT NULL CONSTRAINT [Invite_isValid_df] DEFAULT 1,\n    CONSTRAINT [Invite_pkey] PRIMARY KEY ([id])\n)"
]